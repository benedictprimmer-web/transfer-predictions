# Contradiction Log

Generated from current files plus `reports/data-audit/*`.

| Claim | Reproduced evidence | Label | Resolution |
|---|---:|---|---|
| Canonical transfers are 124,183 | Current `warehouse.transfers_canonical` has 134,147 rows | FACT | 124,183 is an older documented post-merge/pre-raw-enrichment count in `MEMO_stage9_merge.md`. Current table is 134,147. Do not reuse 124,183 for current artifacts. |
| Canonical transfers are 134,147 | `reports/data-audit/canonical_summary.json` reports 134,147 | FACT | This is the current reproducible count. |
| Fee disclosure is 7.4% | Thin R2 `data/transfermarkt/transfers.csv.gz`: 2,584 / 35,139 = 7.35% | FACT | Valid only for the thin source file, not canonical. |
| Fee disclosure is 19.7% | Current canonical all-type positive-fee rate is 28,102 / 134,147 = 20.95% | FACT | The 19.7% claim is close to a previous canonical denominator. Current all-type canonical coverage is 20.95%; permanent-only coverage is 51.59%. |
| Fee-model rows are 16,233 | Current canonical model-ready filter yields 19,859 all-type rows; paid-permanent eligible yields 17,410 | FACT | 16,233 was an older documented model-ready count. Current filters differ after raw enrichment/current table changes. |
| Market-value observations are 445,671 | Current `warehouse.valuations` has 507,815 rows | FACT | 445,671 is stale or from a stricter historical subset not reproduced by current warehouse. |
| Usage/efficiency impact correlation is about 0.077 | Real `validate.stage4` reproduction: WOWY Spearman r=0.0767, CI [0.0301, 0.1250], n=1,614 | FACT | Reproduced. |
| Single-season WOWY is roughly 80% noise | Existing `validate.null_test` command only prints synthetic/summary output, not the exact 80% ratio | INFERENCE | Treat 80% as supported by older memo unless a dedicated table-level null-noise script is added. |
| Gem screen scored about 44% against 43% base rate | RESOLVED post-merge (2026-07-13): `python3 -m validate.talent_gate run` on the combined branch gives gate4 (screen beats base rate) precision 0.235 vs base rate 0.132, CI [0.083,0.122] excludes 0 → PASS. Full verdict across all 4 gates: "edge PRESENT BUT UNPROVEN" (gate2 Antony not runnable, gate3 monotonicity fails) | FACT | main's talent-model commit is now merged (`ead7c73`). See `docs/reconciliation.md` §2. Matches `MODEL_verdict.md` exactly. |
| Talent-minus-price failed to predict repricing reliably | RESOLVED post-merge (2026-07-13): reproduced, matches `MODEL_verdict.md`'s "edge PRESENT BUT UNPROVEN" verdict exactly | FACT | main's talent-model commit is now merged. See `docs/reconciliation.md` §2. |
| NPV board produced no positives among 98 candidates | RESOLVED post-merge (2026-07-13): this was a **stale artifact**, not a live branch disagreement. `money/scout.py`'s NPV logic is byte-identical between the PR branch and `main` (main only added a diagnostic `talent_pctl` column). Re-running `python3 -m money.scout run` against the unchanged code and unchanged input data on the combined branch gives **120 rows, 5 positive `npv_m`** — matching what `main`'s own checked-in artifact already showed. The PR branch's committed `scout_arsenal.csv` (98/0) was never regenerated on that branch; it dates to the `33dae99` initial snapshot commit. | FACT | Do not cite 98/0 as current. Current, reproduced NPV board is 120 rows / 5 positive. See `docs/reconciliation.md` §2-3. |
| Fee model uses random CV | Current `money/fees.py::fit_residuals` uses walk-forward seasons | FACT | The random-CV claim is stale for current code; older docs correctly describe it as a prior/live defect. |
| Haaland and Antony are development sanity cases | `validate.talent_gate` and `money/fees.py` explicitly use them as gates | FACT | They must not be presented as untouched final-test observations. |
| Goalkeeper/defensive valuation coverage is inadequate | FBref and defensive tables exist, but target and coverage artifacts do not establish position-comparable value for GKs/defenders | INFERENCE | Unsupported populations should abstain until a target-specific validation passes. |
| Historical wage, contract, ClubElo, xT freshness claims conflict | Inventory shows Capology/FIFA wage files, current/PIT contract ambiguity, ClubElo through 2026-08-30, and possession gate descriptive-only | FACT | Resolve per artifact and timestamp rule, not by doc prose. |
| "The advanced FBref source is Big-5 2017-22" (`docs/mustermann.md` §0, citing `HANDOFF.md` §1) vs. `reports/v2-full-data/coverage_cube.csv` showing seasons 2010-2026 for the same table | Two DIFFERENT FBref-derived tables exist in this repo. `ingest/fbref_snapshot.py` → `data/fbref_snapshot/` → warehouse view `shots_selling`/turnover source (the ORIGINAL Understat-pivot-era source, genuinely Big-5 2017-22, used for Stage 2/3 turnover data). `ingest/fbref_perf.py` → `data/fbref/perf_player_season.parquet` → warehouse view `fbref_perf` (a LATER, separately-added, 221-column, Estate-B-sourced-then-copied-into-repo table) — its own module docstring states "2010-2026, Big-5, 221 stat cols" in plain text. `docs/mustermann.md` used `fbref_perf` throughout its analysis but cited `HANDOFF.md`'s description of the *other* table (`fbref_snapshot`) for its date range | FACT (self-caught via external review, reproduced independently: `ingest/fbref_perf.py` docstring line 2 says "2010-2026" in the committed source) | **CORRECTED (2026-07-13).** `fbref_perf`'s 2010-2026 range in `coverage_cube.csv` was always correct; the "2017-22" citation in `docs/mustermann.md` was the wrong table. `fbref_perf` is Estate-B-*sourced* but already copied into this repo's tracked `data/fbref/` — unlike `transfer_performance_link_safe`, it does NOT need `ESTATE_B_DIR` present at build time (confirmed: `ingest.warehouse build` succeeds on this checkout without it). See `docs/mustermann.md` §0 (corrected) and `docs/v3-plan.md` §2 for the full source provenance table. |
| `transfer_performance_link_safe` / linked-vs-unlinked prior-performance counts (30,489 / 103,658) | `python3 -m validate.data_audit` on this checkout: table does not exist. It is materialized from `ESTATE_B_DIR` (default `/Users/benrimmer/Downloads/football-transfer-db`), a raw directory outside this repo's tracked `data/`, never committed | BLOCKER | Not a merge conflict — would block a re-run in this environment on either branch. `validate/data_audit.py` and `validate/modelling_contract.py` patched (commit `09f524a`) to mark dependent numbers `UNAVAILABLE_IN_THIS_ENVIRONMENT` instead of crashing or reporting zero. See `docs/reconciliation.md` §4. **CORRECTED (2026-07-13, external review, reproduced):** this table's own definition (`ingest/warehouse.py`: `WHERE perf_season < transfer_season`, explicitly a leak GUARD) makes it a strictly-**prior** feature link, not a destination/future outcome source. `docs/mustermann.md` and `docs/v2-results.md` previously called it "the table that would connect a transfer to the player's destination-season performance" — backwards. Even with Estate B available, this specific view was never going to expose future outcomes; a *separate*, unbuilt future-outcome table (`perf_season > transfer_season`, matched to destination club) would be needed, and does not exist anywhere in this codebase yet. See `docs/v3-plan.md`. |
| Scout board's `talent_pctl` diagnostic column is "wired into" the board | `data/money/scout_arsenal.csv` regenerated on the combined branch: 0/120 rows have a non-null `talent_pctl`, despite `talent_scores.csv` having 24 rows for the same season (2023-2024) | FACT (was INFERENCE, now root-caused) | **CORRECTED (2026-07-13):** the join key is NOT broken — `candidate_universe('2023-2024')` and `talent_scores.csv` do share IDs (verified: `tm_player_id=74230` appears in both; 3/168 raw candidate IDs overlap the 24 talent-scored IDs before further filtering removes even those). The real cause is `impact/talent.py`'s label construction: its target is forward-2-year Δlog(market value), requiring an MV observation ~2 years after the snapshot date. For season 2023-2024 (today: 2026-07-13), only the earliest slice of that season could possibly have a +2y forward observation yet — a right-censoring artifact of the target design, not a crosswalk defect. Confirmed via `impact/talent.py` season counts: 2015-16 through 2022-23 each have 1,100-2,060 rows; 2023-24 has 24. |
| `transfers_canonical.from_league`/`to_league` values are a clean 1:1 league key | `python3 -m validate.v2_full_data` → `reports/v2-full-data/mover_support.csv`: `"La Liga"` (1994-2022, 115 Bundesliga-linked movers) and `"LaLiga"` (2023-2026 only, 27 Bundesliga-linked movers) are two distinct string values for the same competition, split exactly at the league's real-world rebrand year | FACT | Silently fragments any league-keyed grouping (coverage cube, mover-pair counts, domestic percentile, a future league-strength refit) spanning the rebrand. Real defect in `ingest/merge.py`'s canonical normalization, not fixed in this PR — fixing it would change counts already reconciled in `docs/reconciliation.md` and is out of scope. See `reports/v2-full-data/analysis.md` §4. |
| `validate/v2_fee_prototypes.py`'s reported "90% CI [+6.2%,+12.0%]" is a relative-percentage-improvement interval | The function computing it, `bootstrap_rmse_diff`, resampled and returned `rmse0 - rmse1` (an ABSOLUTE difference in log1p-fee units) every time, never divided by `rmse0`. The reported bounds only looked plausible as a percentage because F0's RMSE (0.808) happened to be close to 1.0 | FACT (self-caught via external review, reproduced independently) | **CORRECTED (2026-07-13).** Rewritten: `bootstrap_relative_improvement` now recomputes the ratio inside every resample (a ratio's CI is not its point estimate's CI rescaled), reported separately and correctly labeled from `bootstrap_absolute_diff`. Also switched from row-wise IID bootstrap to a club-block bootstrap (`to_club_id`, 100+ clusters/season) since only 3 distinct evaluation seasons exist (too few for a season-block bootstrap). Also reframed the gate baseline from the naive F0 (fee=MV identity, now F0a, descriptive only) to a temporally-calibrated F0b (log-fee~log-MV, refit per fold) — F1's true relative improvement over the honest baseline is **9.15%** [rel. CI 6.8%,11.5%], not the previously reported 11.3%. See `docs/v2-results.md` and `reports/v2-full-data/fee_prototype_verdict.json`. |
| `impact/evidence.py`'s shrinkage weight `k = within-cell variance / between-cell variance` used directly as `minutes/(minutes+k)` | `k` is a ratio of two variances (individual-player dispersion over cell-mean dispersion) with no minutes unit attached to it; the self-check only verified shrinkage direction, never interval coverage | FACT (self-caught via external review, reproduced independently) | **CORRECTED (2026-07-13).** Rewritten as a proper Gamma-Poisson conjugate model on raw counts + minutes (not the pre-divided rate): prior pseudo-exposure is estimated in "90-minute block" units via method-of-moments so it's dimensionally additive with `minutes/90`. The old bootstrapped-cohort-mean interval (mislabeled as player uncertainty) is replaced with an exact Monte Carlo credible interval drawn from the closed-form Gamma posterior for the player's own rate. New self-check includes a simulation with known true rates: empirical 80% interval coverage came out at 83% (n≈170), the first real calibration test this module has had. See `impact/evidence.py`. |
