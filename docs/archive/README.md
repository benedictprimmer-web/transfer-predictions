# Archive

Completed stage memos, one-shot harvest/research prompts, and point-in-time review docs —
historical record, not the current state. For current state, read (at repo root, in order):
`README.md` → `HANDOFF.md` → `NEXT_STEPS.md` / `DATA_INDEX.md` / `MODEL_verdict.md`.

Moved here 2026-07-13 during a repo-hygiene pass; nothing in `transfer-model-improvement.zip`
references these paths, so the move doesn't affect that bundle. `money/inflation.py`'s
`build()` writes `INFLATION_AUDIT.md`/`INFLATION_FINDINGS.md` here, not to repo root.
