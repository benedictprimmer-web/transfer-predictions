# Table Inventory

Generated by `python3 -m validate.data_audit` at commit recorded in `reports/data-audit/run_manifest.json`.

## Inventory artifacts

FACT - The full file/source inventory is in `reports/data-audit/file_inventory.csv`.

FACT - The warehouse table/view inventory is in `reports/data-audit/warehouse_inventory.csv`.

FACT - Row grain tests are recorded in `reports/data-audit/warehouse_inventory.csv` under `intended_grain_key_tested` and `duplicate_key_rows`.

## Warehouse facts

FACT - `warehouse.transfers_canonical` has 134,147 rows and 34 columns at canonical transfer-row grain. The tested key `transfer_uid` is not unique: 19,113 duplicate-key rows. Source: `reports/data-audit/warehouse_inventory.csv`; filter: all rows; time range: 2009-01-01 to 2026-08-31 by `transfer_date`; command: `python3 -m validate.data_audit`.

FACT - `warehouse.valuations` has 507,815 rows at `(player_id, date)` grain with 0 duplicate-key rows. Source: `reports/data-audit/warehouse_inventory.csv`; filter: all rows; time range: 2000-01-20 to 2026-02-27; command: `python3 -m validate.data_audit`.

FACT - `warehouse.fbref_perf` has 52,951 rows and 222 columns; the tested `(fbref_id, Season_End_Year, Comp, Squad)` key has 47 duplicate-key rows. Source: `reports/data-audit/warehouse_inventory.csv`; filter: all rows; command: `python3 -m validate.data_audit`.

FACT - `warehouse.shots` has 539,971 rows, 18 columns, and 126 duplicate rows under the tested shot key. Source: `reports/data-audit/warehouse_inventory.csv`; filter: all rows; time range: 2014-08-01 to 2025-01-04.

FACT - `warehouse.shots_selling` has 234,800 rows and 523 duplicate rows under the tested shot key. Source: `reports/data-audit/warehouse_inventory.csv`; filter: all rows.

FACT - `warehouse.clubelo_history` has 771,384 rows at `(club, date)` grain with 0 duplicate-key rows. Source: `reports/data-audit/warehouse_inventory.csv`; time range: 1939-01-08 to 2026-08-30.

FACT - The file inventory records source/licence risk and producer script for each modelling-relevant data artifact. Source: `reports/data-audit/file_inventory.csv`; command: `python3 -m validate.data_audit`.

## Rebuild and redistribution

INFERENCE - Most large artifacts are tracked or locally present and can be profiled from a clean checkout only if Git LFS/raw files are present. Network/raw-source rebuild was not attempted because this audit was scoped to current files and no raw-data expansion.

INFERENCE - Public/commercial redistribution is not cleared for Transfermarkt-derived, FBref, Understat, Capology, and ClubElo artifacts. See `reports/data-audit/file_inventory.csv` and `LICENCES.md`.

